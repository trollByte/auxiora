apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: auxiora

resources:
  - namespace.yaml
  - deployment.yaml
  - service.yaml
  - ingress.yaml
  - hpa.yaml
  - configmap.yaml
  - secrets.yaml
  - redis.yaml
  - postgres.yaml
  - cronjob.yaml

commonLabels:
  app.kubernetes.io/managed-by: kustomize
